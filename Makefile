# A file for Mac User (sorry I only know the very basic of this)
# to clean up unneccessary stuff -
# in this case .DS_Store (Mac internal stuff), .RData and .Rhistory
clean:
				rm -rf .DS_Store .RData .Rhistory
